[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "todo-cli"
version = "0.1.0"
description = "A terminal-based task manager"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[project.scripts]
todo = "To_do_list:main"